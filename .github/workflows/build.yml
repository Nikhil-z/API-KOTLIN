name: Deploy to Subdomain

on:
  push:
    branches:
      - main  # Adjust to your main branch name

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up SSH
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: 65.2.171.237  # Replace with your server's hostname or IP address
          username: ubuntu  # Replace with your SSH username
          key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: List Directories on Remote Server
        run: |
                ssh -i ${{ secrets.SSH_PRIVATE_KEY }} ubuntu@ip-172-26-1-31:/www/wwwroot/api.geeknuts.in  << EOF
                ls -l
                # Additional deployment steps if needed
                ls
                EOF
        env:
                SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}


      - name: Build JAR
        run: ./gradlew build

      - name: List Local Directories
        run: |
          ls build/libs
          ls src
     

      - name: Transfer JAR file to Server
        run: |
          scp -i ${{ secrets.SSH_PRIVATE_KEY }} build/libs/api-0.0.1.jar ubuntu@65.2.171.237:/www/wwwroot/api.geeknuts.in
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
    
      - name: SSH into Remote Server and Run JAR
        run: |
          ssh -i ${{ secrets.SSH_PRIVATE_KEY }} ubuntu@65.2.171.237 'cd /www/wwwroot/api.geeknuts.in && java -jar api-0.0.1.jar'
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
